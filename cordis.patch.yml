# Optional conversation/workspace rewind plugin. The package owns both the host
# command and the browser button; the row name must match package.json.
- insert:
    - id: dsh-session-timeline
      name: '@x1a0f3n9/dsh-session-timeline'
